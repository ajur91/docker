version: '3.8'

networks:
    net_database:
        name: net_database
        driver: bridge

services:
    adminer:
        image: adminer
        restart: always
        ports:
            - 8082:8080
        networks:
            - net_database