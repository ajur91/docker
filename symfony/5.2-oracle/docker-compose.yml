version: '3.8'

networks:
    net_betox:
        external:
            name: net_betox

services:
    web:
        image: betox/symfony:5.x-oracle
        ports:
            - 85:80
        volumes:
            - .:/var/www/html
        networks:
            - net_betox